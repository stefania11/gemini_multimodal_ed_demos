import React from 'react';
import ReactDOM from 'react-dom';
import App from './math_demo';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
